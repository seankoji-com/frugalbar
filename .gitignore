# .gitignore for Swift / Xcode / macOS
.DS_Store
*.swp
*.lock
.build/
.swiftpm/
DerivedData/
*.xcodeproj
*.xcworkspace

# Claude Code runtime state — never commit
.claude/worktrees/
.claude/jobs/
.claude/settings.local.json

# Local code coverage and mutation-proof artifacts
coverage/
